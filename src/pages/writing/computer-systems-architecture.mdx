import ArticleLayout from "@/components/layouts/ArticleLayout";

export default (props) => (
  <ArticleLayout title={"architecture."} date={"16 April, 2023"} {...props} />
);

This is a post in a series about computer systems, starting from architecture. In the next couple of parts I will be covering Operating Systems (including writing one from scratch, bootloader and all),
