import ArticleLayout from "@/components/layouts/ArticleLayout";

export default (props) => (
  <ArticleLayout title={"Learning to struggle"} {...props} />
);

I think every one of us has atleast toyed with the idea of _being a genius_ at some point in their lives. Whether it was because we were able to dazzle at high school, or because of the fact that we were able to pick up things very easily.

# Dealing with mediocrity

I know I certainly did. For most of my life I was able to do the things I wanted
