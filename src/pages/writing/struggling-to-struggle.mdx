import ArticleLayout from "@/components/layouts/ArticleLayout";

export default (props) => (
  <ArticleLayout
    title={"Learning to struggle, and accepting a glass half full"}
    {...props}
  />
);

I think every one of us has atleast toyed with the idea of being a genius at some point in their lives. Whether it was because we were able to dazzle at high school,
